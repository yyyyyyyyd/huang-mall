spring:
  application:
    name: huang-product
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml # 指定配置文件为yaml格式
        namespace: huang-dev
  profiles:
    active: dev

